# AUDIT 1000% — SPARDA (2026-07-17)

> World-class repreneur/CTO/investor/growth audit run by a parallel session, then **verified
> live against the code** by the Claude session on `claude/current-task-u45a4d`. This file
> records the findings AND their verified outcome — because an audit read against a stale corpus
> is worse than none. Confidence tags kept: `[MESURÉ]` (re-run today), `[LU]` (from a dated doc).

## Verified outcome (what was real vs stale)

| # | Finding | Verified status |
|---|---|---|
| 1 | `PROVEN` at ~0% coverage (the "cardinal sin") | ✅ **STALE — already fixed.** The auditor read the 2026-07-13 corpus (`[LU]`). Measured today: 0–4% → SURFACE, 23% → PARTIAL, 90% → PROVEN. The `COVERAGE_FLOOR` + PARTIAL rung (E-044) closed it; pinned by `tests/verdict-partial.test.js`. |
| 5 | Injection filter bypassed by homoglyph / zero-width | ✅ **REAL — fixed (E-045).** Verified live (`Ignоre…` Cyrillic о, `ignore<zwsp>previous`). Now normalized (NFKC + homoglyph fold + invisible strip) before the denylist; 6 evasion regression cases. |
| 3 | `--dir`/`--cwd` not wired | ✅ **REAL — fixed (v0.58).** `opts.cwd` now resolves `--dir`/`--cwd`. |
| 6 | Zero Semgrep/CodeQL/Snyk positioning | ✅ **DONE** — `docs/COMPETITION.md` SAST section. |
| 7 | "sparda" SEO collision (Devil May Cry) | ✅ **DONE** — SEO note in COMPETITION.md; target long-tail, keep `sparda-mcp`. |
| 2 | 15 flat commands, no real `--help` | ✅ **DONE (v0.59)** — per-command `--help`. |
| 4 | README numbers not CI-gated to bench | ✅ **DONE (v0.59)** — `npm run bench:check` in CI. |
| 10 | No public trust log | ✅ **DONE** — `docs/TRUST-LOG.md`. |
| — | `js-tokens` 5th, caret, invalid dep | ✅ **caught + removed** — back to 4 exact-pinned. |
| — | Dogfooding Reach/Publish | ⏳ blocked on repo access. |

The audit's ranked-#1 blocker was stale; its real critical finding (#5) is fixed. The rest is
shipped or filed. Net: the audit was high-quality and its confidence calibration is exactly what
let us catch its own stale item.

## Part A — LLM-assisted blind-spot resolution (post-adoption engine spec)

The audit's forward-looking engine direction (frozen by the adoption kill-list until the funnel
turns; free-first per the playbook — never behind a key/tier). Full sourced spec, with the three
papers it rests on, lives in **`docs/RESEARCH-AND-10X-IDEAS-2026-07-17.md` (Partie 1, papiers 2+3
and idea A)**. The soundness-critical rule, restated here so it is never lost:

> An LLM may only produce a **resolution hint** (where to look) or a **candidate classification**,
> never an assertion of behavior. SPARDA must **structurally re-verify** the hint against the real
> graph before anything enters the proof. A guard is `verified` only when SPARDA itself proves a
> deny path — an LLM hint that leads there earns `verifiedVia: 'llm-guided'` provenance, never a
> shortcut. Skipping this step is exactly what produced E-022/E-025/E-026 (false PROVEN). This is
> not optional prudence — the literature (Thakur et al., STTT 2025) identifies verify-before-admit
> as the critical step.
