# 2026-07-15 — ADR-054 phase 2: convergence + Python depth (0.33.0, 0.34.0)

**Scope:** execute the phase-2 program in the playbook's candidate order — E-034
fall-through, strategy convergence, Wave 2b Python depth; defer the ORM table honestly.
**Commits:** `0610b64` (E-034), `548ba07` (convergence), `fbf8a8c` (Wave 2b) ·
**Branch:** `claude/current-task-u45a4d` · **Tests:** 558 ✓ (3 skip), ESLint 0, Prettier clean

## Done

- **E-034 → RESOLVED (0.33.0):** detection falls through Express → Nest/Medusa when those
  markers exist instead of hard-failing; immich/twenty compile through the full pipeline again.
- **Convergence (0.33.0):** DI is now a receiver kind of the ONE `followCalls`; the separate DI
  machine deleted (~90 lines). Enrichment verified finding-by-finding on the real corpus:
  - twenty: 14→74 dbEff, cov 8%→47%, PROVEN→NOT_PROVEN F=5 (OAuth-callback
    IRREVERSIBLE_OBSERVABLE class — rule-true: external token exchange + state write, no
    compensation path).
  - immich: 283→431 dbEff, cov 88%→92%, F=2→6 — incl. a REAL missing `@Authenticated` on the
    alpha `POST /admin/database-backups/start-restore` (source-checked: every sibling endpoint
    carries the decorator; the route's gating hides in service business logic — unprovable).
    **Candidate responsible disclosure for Zak to decide.**
- **Wave 2b (0.34.0):** the resolve.js contract implemented in `fastapi_extract.py` (one walk:
  singletons, imported classes/aliases, bare imported functions, `self.<m>()`, DI bindings;
  deep-scanned `Depends()` providers; SQLAlchemy 2.0 shapes). **open-webui 456r: 0 → 1353 db
  effects, coverage 0% → 77%, PROVEN unchanged, 3.3s.**
- Fixtures: `ubg-nestjs-express-dep`, `ubg-nestjs-converged`, `ubg-fastapi-deep` (+9 tests).
- Corpus baselines re-pinned at v0.34.0 in the playbook §0.

## Not done / deferred

- **ORM import-root provenance** — the last ADR-054 increment. Wide surface (module import
  context into `extract.js#scanFunction` + effect provenance schema + consumers); spec written
  in the playbook ("Next increment"); part-25 precedent applied: recorded, not rushed.

## Decisions made

- Enrichment gate interpretation: fixture verdicts must stay identical; on the real corpus a
  finding-count move is allowed ONLY after per-finding source verification (all passed).
- Version arc: 0.33.0 (convergence + E-034), 0.34.0 (Python depth). ADR-054 addenda 1–2 in
  DECISIONS.md.

## Bugs hit

- **Engine-instance shadowing** in `extractNest` (`resolver` collided with the GraphQL
  `@Resolver` decoratorArg local) — fixture oracle caught it instantly; renamed `engine`.
- **E-035 (new, ERRORS.md):** spawnSync's 1 MiB default `maxBuffer` killed the Python child
  mid-write on open-webui and surfaced truncated valid JSON as a "parse failure". 64 MiB now.
- **novu at HEAD reads 0 routes** (old AND new code — upstream layout drift, honest NO_PROOF;
  its playbook corpus row needs a re-derived app dir next time someone needs novu).

## Notes for the next session

- Order stays: ORM import-root (own session, spec ready) → Wave 3 taint (E-029 tripwire!) →
  ADR-P5 golden bench (protects everything) → guard dominance (parallel quick win) → Reyna loop.
- The immich `start-restore` finding is exactly the disclosure-grade material part 9 turned into
  the Prisma issue — worth surfacing to Zak as outreach ammo (his call, never automatic).
- open-webui's PROVEN now stands on 77% coverage — the "hollow PROVEN" era of FastAPI apps is
  over; blindspots on the remaining 23% are the honest next ask there.
