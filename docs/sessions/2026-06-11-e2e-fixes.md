# 2026-06-11 — E2E campaign ingested + all findings fixed

**Scope:** Ingest the local desktop session's full E2E campaign (32/33 green
through a real MCP client — the publish blocker), fix every finding it raised,
ship regression tests.
**Commits:** see branch · **Branch:** `claude/sparda-e2e-desktop-test-bkhfaj` · **Tests:** 29/30 green (1 env-skip), 4 new

## Done

- **Ingested the campaign**: `debrief_phase{1,2,3}.md` committed under
  `docs/sessions/` (same paths as the local clone's unpushed commits, so a
  future merge dedups). Phase 1: 13/13 protocol checklist via scripted
  JSON-RPC client with sampling + elicitation. Phase 2: 12/12 hardened app,
  ESM + CJS. Phase 3: 8/9 lifecycle. Carry-over (hard rule #5) intact — no P0.
- **E-010 fixed** — `remove` reverts the `.gitignore` edit. `ensureGitignore`
  (both generators) returns `created`/`appended`/null; the manifest records it
  (carried across re-init); `remove` undoes the exact edit. init→remove is now
  byte-for-byte clean on the whole tree, not just injected code. 2 regression
  tests (created → file deleted; appended → byte-exact restore incl.
  no-trailing-newline worst case + carry-over of the record).
- **E-011 fixed** — `detectExpressPort` reads the fallback literal of
  `process.env.*PORT* ?? / || <n>` (incl. `Number(...)` wrapper). 4-case
  regression test.
- **E-012 fixed** — `runDoctor` returns `{ healthy }`; CLI sets
  `process.exitCode = 1` on critical `✗`. Regression test (pre-init healthy,
  dead host unhealthy).
- **Non-TTY `remove` guard** — without `--yes` and without a TTY it now throws
  a clean `USER` error (`--yes` hint) instead of clack's `EBADF` crash.
- **E-013** — Sentinel sync test gets an explicit 30s timeout; root cause of
  the desktop flake was a local `npm install` *inside* the fixture (repo ships
  no fixture node_modules). Rule recorded in ERRORS.md.
- `docs/ERRORS.md`: entries E-010 → E-013. `docs/HANDOFF.md` rewritten:
  publish blocker closed, next step = merge + npm publish.

## Not done / deferred

- P2.2 query params in `inputSchema` — backlog (semantic pass or AST
  `req.query.X` detection).
- README line about `remove` not uninstalling the post-commit hook.
- Desktop cleanup (PID 16820 on :3344, `Desktop/app-demo`,
  `Desktop/sparda-user-test`) — owner's machine, owner's call.

## Decisions made

- The `remove` byte-for-byte promise applies to the **whole tree** — the
  "minus a .gitignore line" carve-out is dead (manifest `gitignore` field).
- `doctor`'s exit code is part of its contract (CI-gateable).

## Bugs hit

- E-010, E-011, E-012, E-013 — all in `docs/ERRORS.md` with rules.

## Notes for the next session

- Local desktop clone `main` is **ahead 4 / behind 1**: its commits (debriefs
  + a HANDOFF edit) are content-superseded by this branch. On merge to main,
  resolve HANDOFF in favor of this branch; the debrief files are identical.
- After npm publish: delete `GEMINI-BRIEF.md` + `LOCAL-SESSION-BRIEF.md`, flip
  HANDOFF's publish line, and consider 0.3.1 only if anything was published
  before these fixes (it wasn't — 0.3.0 ships with them).
