# 2026-06-12 — v0.4 complete: recycling gauge + idle harvester + condenser + crystallization

**Scope:** HANDOFF next-step #1 — R4.1 recycling counter, R4.4 idle harvester,
ROADMAP round 2 brick 1 (call-sequence recording, deterministic, default-off),
then brick 2 (crystallization) added in the same session per owner go.
**Commits:** see branch · **Branch:** `claude/next-todo-step-76re5c` → merged to `main` (owner instruction) · **Tests:** 45/45 green (FastAPI runtime included)

## Done
- **R4.1 — recycling gauge**, both router templates (hard rule #6): runtime
  counters `servedByCircle`/`paidFull` + `ratePct` in `/mcp/stats.recycle`.
  `paidFull` increments right before the upstream fetch; `servedByCircle` on
  quarantine blocks (the doomed host call was never paid). Bridge side: avoided
  sampling calls counted against the real `DIAGNOSIS_TOKENS`/`SEMANTIC_TOKENS`
  budgets; lifetime savings **derived** from antibody `hits` (zero new state).
  Exposed in `sparda_get_context → recycling` and `sparda_info`.
- **R4.4 — idle harvester** `src/server/idle.js`: `monitorEventLoopDelay`-based
  quiet detection, one job per tick, bounded queue (refuses at cap), starvation
  guard (`maxWaitMs`), synchronous `flush()` wired to bridge `onclose`.
- **R2.1 — sequence condenser** `src/server/condenser.js`: Labs, default OFF
  (`labs.recordSequences` in sparda.json, env `SPARDA_RECORD_SEQUENCES=1` for a
  session). 20-call ring, deterministic value match (most recent producer wins),
  noise floor (strings 2–200 chars; numbers ≥ 10 unless key matches `/id$/i`),
  chains ≤ 5, circuits ≤ 30 (least-observed evicted), merge-write persistence,
  `onCircuit` announce at 3 observations via MCP logging. **Structure only on
  disk — never payload values.**
- `labs` added to the sacred carry-over (both generators, manifest.js comment,
  CLAUDE.md rule 5, ADR-008 extension noted in ADR-014).
- Tests: recycle assertions in Express + real-uvicorn runtime suites; new 7e
  (harvester) + 7f (condenser) suites; bridge E2E circuit detection
  (`post_items → get_items` via `id`); `labs` carry-over; generated-router
  parse check across all 4 Express variants (placeholders can no longer slip).
- **R2.2 — crystallization** `src/server/crystallize.js` (second pass, same
  session): condenser links now record `fromKey` (output key the matched value
  lived under — still structure-only); at the threshold, `onCircuit` →
  `crystallizeCircuit` in the bridge: eligibility = every step an enabled GET
  + every link has a `fromKey`; sampling names it (`normalizeCompositeName` +
  `sanitizeDescription`, `CRYSTAL_TOKENS`=150, one call per circuit ever) with
  a deterministic `circuit_a_then_b` fallback; `composite` persisted in the
  circuit; registered in tools/list (`[Labs circuit ×N]` badge, schema = union
  of step params minus auto-fed); `sendToolListChanged()`. Execution:
  `runComposite` replays the chain through the router, auto-feeds via
  `findByKey(prevOutput, fromKey)`, stops at first failure with a trace.
  Composites from past sessions re-registered (and re-validated) at startup.
- Docs: ARCHITECTURE (schema + bridge organs + file map), ADR-013/014/015,
  TESTING suite map (7e/7f/7g), README v0.4 section, ROADMAP §5.4 → ✅ and
  round-2 bricks 1-3 ✅, version 0.4.0.
- **CLI identity pass** (owner-driven, from his HTML mockup): `src/ui/style.js`
  — zero-dep ANSI (hard rule #8 intact): per-char violet→cyan truecolor
  gradient on the SPARDA banner (256-color ramp fallback, plain on
  NO_COLOR/non-TTY/CI), green ✓ / red ✗ in the tools box (clack strips ANSI
  for width — alignment safe), single-pass JSON highlighter for the Claude
  Desktop config block. Human commands only — the bridge stays plain
  (stdout = protocol, rule #2). Suite 7h covers on/off/fallback modes.

- **P2 backlog cleared** (third pass, same session — owner away from his
  desktop, E2E deferred to one one-shot pass): query params AST-detected on
  Express inline handlers (`req.query.x`, `['x']`, destructuring, custom req
  names, deduped vs path params — FastAPI already extracted them from
  signatures); MCP `annotations` on every tool + composites + built-ins
  (readOnly/destructive/idempotent/openWorld, HTTP-mapped); `remove` now
  uninstalls the post-commit sentinel via `removeHook` (created → deleted,
  appended → byte-for-byte restore, marker-less → no-op).

- **R4.2 purity detector** (fourth pass, same session): both router templates
  fingerprint GET+200 bodies (FNV-1a / crc32, 64 KB cap) under sorted-args
  argsigs → `pure` (≥3 identical repeats) / `volatile` (any mismatch) /
  `erasing` (non-GET) / `unknown`. Bounded 20 argsigs/tool, runtime-only,
  exposed in `/mcp/stats.purity` + get_context hint. ADR-017. Runtime
  assertions on both frameworks (pure /health, volatile random route,
  erasing POST, unknown for the 500 route).

## Not done / deferred
- R2.4 re-mapping on code change (today: a changed step un-registers its
  composite at startup; no diff-and-remap).
- R4.3 flywheel (content-addressed cache of proven-pure responses) — next
  brick, now unblocked by the purity detector. Design constraint to respect:
  a cache never replaces a real answer (survival rule §1) — serve-stale only
  as prediction/prewarm with idle revalidation, semantics to be decided in
  its ADR.
- npm publish of 0.4.0 — owner's call, after the one-shot manual E2E
  (owner currently away from his desktop).

## Decisions made
- ADR-013 (gauge semantics: measured, runtime-only, estimates pinned to real
  budgets), ADR-014 (condenser default-off, structure-only, bounded),
  ADR-015 (crystallization GET-only, fallback-first, re-validated at start).
- Condenser/harvester/crystallizer live in the **bridge**, not the router: the
  host process never pays for them (hard rule #1), one implementation instead
  of two templates, and the bridge *is* the session (R2.1 says "per session").

## Bugs hit
- None non-trivial. One self-caught design slip: link detection initially
  iterated args-then-ring, which could bind to a stale producer — flipped to
  ring-descending so the most recent producer wins.

## Notes for the next session
- The bridge test's mock host: the condenser does NOT touch `/mcp/events`
  (E-005 safe), and the circuit polls read `sparda.json` from outside —
  guard against mid-write reads with try/catch (already in the test).
- `process.env` gates in the router are read at import time; the condenser's
  env gate is read at bridge start. Re-init does not touch `labs`.
- Crystallization with REAL sampling has no automated coverage (test client
  has no sampling capability → fallback path) — exercise it in the next
  manual E2E before publishing 0.4.0.
- Owner inputs 2026-06-12: desktop cleanup fully done (PID :3344 killed,
  `app-demo` deleted); Shadow pricing and `localKey` hardening still open.

> Remember: rewrite `docs/HANDOFF.md` before committing this file. (done)
