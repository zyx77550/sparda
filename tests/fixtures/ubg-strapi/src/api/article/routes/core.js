'use strict';

// The dominant Strapi idiom: an auto-generated CRUD router. Five routes (find/findOne/create/
// update/delete) are implied by this single call — a registry SPARDA must partially evaluate.
const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::article.article');
