{
  "name": "sparda",
  "owner": { "name": "Residual Labs", "url": "https://github.com/zyx77550/sparda" },
  "metadata": {
    "description": "SPARDA — the trust layer for AI-written code. AI writes. SPARDA proves.",
    "version": "0.1.0"
  },
  "plugins": [
    {
      "name": "sparda-gate",
      "source": "./integrations/claude-code-plugin",
      "description": "The behavior gate for AI edits — proves each edit lost no auth guard, dropped no route, grew no blast radius. Deterministic, <2s, offline, zero API key.",
      "category": "security",
      "keywords": ["security", "proof", "authorization", "guard", "behavior-graph"]
    }
  ]
}
